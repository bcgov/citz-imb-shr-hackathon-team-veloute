import healthRouter from "./health-router";
import csvUploadRouter from "./csv-upload-router";
import dbRouter from "./db-router";

const router = {
  healthRouter,
  csvUploadRouter,
  dbRouter,
};

export default router;
